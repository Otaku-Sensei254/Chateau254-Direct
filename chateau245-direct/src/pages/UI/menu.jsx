import { FiPlus, FiSearch, FiTruck } from 'react-icons/fi';

const Menu = ({ items, categories, filter, setFilter, query, setQuery, addToCart, cartCount, onCart, onViewItem, user }) => { return <main className="content-page menu-page"><div className="section-heading"><div><p className="eyebrow">Welcome back, {user?.full_name || ''}</p><h1>What are you<br /><em>craving today?</em></h1></div><span className="order-badge"><FiTruck /> 45 min delivery</span></div><div className="search-box"><FiSearch /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search menu..." /></div><div className="filter-row">{categories.map((category) => <button className={filter === category ? 'active' : ''} key={category} onClick={() => setFilter(category)}>{category}</button>)}</div><div className="menu-grid">{items.map((item) => <article className="menu-card" key={item.id}><button className="menu-card-view" onClick={() => onViewItem(item)} aria-label={`View ${item.name}`}><div className="food-image" style={{ backgroundImage: `url(${item.image})` }}><span className="category-tag">{item.category}</span></div><div className="menu-card-body"><div><h2>{item.name}</h2><p>{item.description}</p><strong>KES {item.price.toLocaleString()}</strong></div></div></button><button className="add-button" aria-label={`Add ${item.name}`} onClick={() => addToCart(item)}><FiPlus /></button></article>)}</div>{!items.length && <div className="empty-state">No dishes found. Try another search.</div>}{cartCount > 0 && <button className="floating-cart" onClick={onCart}>View cart <span>{cartCount}</span></button>}</main>; };

export default Menu;
